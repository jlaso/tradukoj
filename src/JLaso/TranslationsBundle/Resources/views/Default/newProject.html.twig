{% extends '::base.html.twig' %}

{% block stylesheets %}
    <style type="text/css">
        body {
            padding-top: 160px;
            padding-bottom: 40px;
            background-image: url("{{ asset('bundles/translations/resource/img/bg.png') }}");
        }

        .sidebar-nav {
            padding: 9px 0;
        }

        .error ul{
            list-style: none;
        }
        .error li{
            color: red;
            font-weight: bold;
        }
    </style>
{% endblock stylesheets %}

{% block body %}
    <div class="container">

        <div class="row">

            <div class="span4 offset4">
                <div class="navbar">
                    <div class="navbar-inner">
                        <div class="container">
                            <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
                                <span class="icon-bar"></span>
                                <span class="icon-bar"></span>
                                <span class="icon-bar"></span>
                            </a>
                            <a class="brand" href="#" style="color: #ffffff;">{{ "new_project.title"|trans|raw }}</a>

                            <div class="nav-collapse">

                            </div>
                            <!-- /.nav-collapse -->
                        </div>
                    </div>
                    <!-- /navbar-inner -->
                </div>

                <form class="well" action="{{ path('new_project') }}" novalidate  method="post" {{ form_enctype(form) }}>
                    <div class="input-prepend">
                        <div class="error">{{ form_errors(form.project) }}</div>
                        <span class="add-on"><i class="icon-envelope"></i></span>
                        {{ form_widget(form.project, {'attr' : {'class':'span3'} }) }}
                    </div>
                    <div class="input-prepend">
                        <div class="error">{{ form_errors(form.name) }}</div>
                        <span class="add-on"><i class="icon-envelope"></i></span>
                        {{ form_widget(form.name, {'attr' : {'class':'span3'} }) }}
                    </div>
                    <div class="input-prepend">
                        <div class="error">{{ form_errors(form.api_secret) }}</div>
                        <span class="add-on"><i class="icon-envelope"></i></span>
                        {{ form_widget(form.api_secret, {'attr' : {'class':'span3'} }) }}
                    </div>
                    <div class="input-prepend">
                        <div class="error">{{ form_errors(form.managed_locales) }}</div>
                        <span class="add-on"><i class="icon-envelope"></i></span>
                        {{ form_widget(form.managed_locales, {'attr' : {'class':'span3'} }) }}
                    </div>

                    {{ form_rest(form) }}

                    <div class="container">
                        <div class="row">
                            <div class="span12">&nbsp;</div>
                            <div class="span12">
                                <input type="submit" class="btn btn-danger" value="{{ "new_project.register_button"|trans|raw }}" />
                                <a href="{{ path('user_index') }}" class="btn btn-primary">
                                    {{ "register.cancel"|trans|raw }}
                                </a>
                            </div>
                            <div class="span12">&nbsp;</div>
                            <div class="span12">
                                <span>{{ "register.privacy_policy"|trans({'url':"{# {{ path('privacy_policy') }} #}"})|raw }}</span>
                            </div>
                        </div>
                    </div>

                </form>
            </div>

        </div>

    </div>
{% endblock body %}
